import React, { Component } from "react";
import { render } from "react-dom";

import ReactRouter from './index'

function App () {
    return (
        <div>
            <ReactRouter />
        </div>
    );
}

export default App;
